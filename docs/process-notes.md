# harmonica-session-review — Process Notes

## 2026-05-23 — created + archived in one session
- **Done:** Created this repo as a standalone Claude Code skill (renamed from the `review-session` skill living in claude-config). Pushed to github.com/harmonicabot/harmonica-session-review (public, MIT, with install.sh / install.ps1 following the harmonica-chat pattern). Then in the same session, recognized it duplicated the lighter `review` lifecycle mode already in harmonica-chat — the rationale for separation didn't hold up. Folded the full framework BACK into harmonica-chat's `review` subcommand (now harmonica-chat v2.11.0+, eventually v3.3.0 via the impeccable-pattern split). Archived this repo on GitHub via `gh api -X PATCH repos/harmonicabot/harmonica-session-review -F archived=true`. README rewritten as a deprecation notice pointing to harmonica-chat. Local skill at `~/.claude/skills/harmonica-session-review/` removed; install path preserved for anyone who already ran the curl install.
- **Decisions:** Kept the source dir at `~/claude-project/harmonica-session-review/` for git history. Archived (read-only) vs deleted on GitHub — preserves the install path for prior installers; new installers won't find it via search but the existing URLs still resolve.
- **State:** Repo archived on GitHub at 7c1ec11. Source dir intact locally. Skill not installed anywhere active.
- **Next:** None. Don't revive unless harmonica-chat's `review` mode proves insufficient AND the duplication concern can be resolved.
- **Lesson:** When considering whether to publish a separate repo for something that overlaps with an existing tool, do the duplication/separation analysis BEFORE creating the repo — not after creating it and publishing. The user surfaced the duplication question in conversation that should have happened first.
